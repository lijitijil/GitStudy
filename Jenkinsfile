pipeline {
    agent any

    stages {
        stage('Buildjen') {
            steps {
                echo 'Build  Jenkins job'
            }
        }
        stage('Testjen') {
            steps {
                echo 'Test Jenlins job'
            }
        }
        stage('Deploy jen') {
            steps {
                echo 'Deploy Jenkins job'
            }
        }
    }
}
